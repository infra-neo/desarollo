# API Configuration
VITE_URL_API=http://localhost:8005

# Package Analytics Configuration
# Disable Scarf analytics to prevent network calls to scarf.sh (blocked by firewall)
# The swagger-client package includes @scarf/scarf which sends telemetry data
SCARF_ANALYTICS=false

# Application Configuration
# Add other environment variables as needed
